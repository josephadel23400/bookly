import 'package:flutter/material.dart';

class ForLaterTab extends StatelessWidget {
  const ForLaterTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
